// Wind-animated cutout, unshaded (flat colour, every plane the same brightness).
shader_type spatial;
render_mode unshaded, cull_disabled, depth_draw_opaque;
#include "res://scenes/props/tree/shaders/wind_cutout.gdshaderinc"
